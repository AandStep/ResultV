.gradle/
build/
local.properties
.idea/
*.iml
.cxx/
captures/
.externalNativeBuild/
.kotlin/

# Built artifacts (regenerated via `gomobile bind` in mobile/)
libs/*.aar
libs/*-sources.jar

# Release signing — credentials live in keystore.properties (copy from
# keystore.properties.example and fill in real values)
keystore.properties
*.keystore
*.jks
# Base64 dump of the keystore for pasting into CI secrets — never commit
*.keystore.b64.txt
