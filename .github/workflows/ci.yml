name: CI

on:
  pull_request:
    branches: [main, dev]
  push:
    branches: [main, dev]

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install Linux build deps
        if: matrix.os == 'ubuntu-latest'
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          set -eux
          sudo find /etc/apt \( -name '*.sources' -o -name '*.list' -o -name '*.sources.list' \) \
            -type f -exec sed -i 's/azure\.archive\.ubuntu\.com/archive.ubuntu.com/g' {} +
          sudo tee /etc/apt/apt.conf.d/99-ci-apt >/dev/null <<'EOF'
          Acquire::Retries "5";
          Acquire::http::Timeout "120";
          Acquire::https::Timeout "120";
          Dpkg::Use-Pty "0";
          EOF
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential pkg-config \
            libgtk-3-dev libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev

      # `main.go` has `//go:embed all:frontend/dist`, which fails to compile
      # if the directory is empty. Build the frontend first so vet/test see
      # real files.
      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      # Compile with the same feature tags the released binaries use, so CI
      # exercises the code that actually ships. Some behaviour is tag-dependent
      # (e.g. the gRPC transport, see internal/proxy/grpc_transport_full.go), and
      # without this vet/test would check a build nobody runs. Read straight from
      # wails.json so the two can never drift; node is already set up above.
      - name: Resolve build tags
        id: tags
        shell: bash
        run: |
          tags=$(node -e 'process.stdout.write(require("./wails.json")["build:tags"])')
          echo "tags=$tags" >> "$GITHUB_OUTPUT"
          echo "build tags: $tags"

      # unsafeptr trips on legitimate Windows-callback patterns (e.g.,
      # converting LPARAM to a struct pointer). The kernel guarantees pointer
      # lifetime for the message handler, so the warning is a false positive.
      - name: go vet
        run: go vet -unsafeptr=false -tags "${{ steps.tags.outputs.tags }}" ./...

      - name: go test
        run: go test -tags "${{ steps.tags.outputs.tags }}" ./... -count=1
